
<table>
  <tr>
    <td>Name :</td>
    <td><%= @cat.name %></td>
  </tr>
  <tr>
    <td>Color: </td>
    <td><%= @cat.color %></td>
  </tr>
  <tr>
    <td>Age: </td>
    <td><%= @cat.age %></td>
  </tr>
  <tr>
    <td>Description: </td>
    <td><%= @cat.description %></td>
    </tr>
  <tr>
    <td>Sex: </td>
    <td><%= @cat.sex %></td>
  </tr>
</table>

<h3>Requests!</h3>

<% @cat.cat_rental_requests.each do |request| %>
<%= request.start_date %> |
<%= request.end_date %>
  <input type="submit"  value="Approve request!" on_click = request.approve! >
<br>
<% end %>
